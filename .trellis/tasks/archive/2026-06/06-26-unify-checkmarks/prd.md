# 统一文档中完成状态的视觉标记

## Goal

将所有 `[x]` / `[ ]` Markdown 复选框替换为 emoji 风格（`✅` / `⏳`），与"今日完成"列表视觉统一，在纯文本编辑器中也清晰易读。

## Requirements

### 替换规则
- `- [x]` → `- ✅`
- `- [ ]` → `- ⏳`

### 涉及文件

1. **`projects/01-simple-agent/README.md`**（12 处 `[x]`，无 `[ ]`）
   - 第 1 步环境搭建：3 处
   - 第 2 步 Agent 类：3 处
   - 第 3 步工具系统：3 处
   - 第 4 步整合测试：3 处

2. **`projects/01-simple-agent/notes.md`**（3 处 `[x]`，无 `[ ]`）
   - 待提炼清单：3 处

## Acceptance Criteria

- [ ] 两个文件中不再出现 `[x]` 或 `[ ]`
- [ ] 已完成项显示为 `✅`
- [ ] 未完成项显示为 `⏳`（当前无此情况，但规则需一致）

## Out of Scope

- 不修改 `notes/` 目录下的文件（无复选框）
- 不修改 `progress/` 目录（已用 `✅` emoji）
- 不修改主 `README.md`（无复选框）
- 不修改代码文件
