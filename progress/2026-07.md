# 2026年7月学习进度

## 2026-07-02（周四）完成 08-long-term-memory 🎉

### 今日完成
- ✅ 需求脑暴：确定 ChromaDB（本地持久化）+ 自定义 embedding API 端点 + 模拟3 session 演示
- ✅ 实现 `long_term_memory.py`：`OpenAICompatibleEF`（自定义 embedding，无本地模型）+ `LongTermMemory`（store/retrieve/clear/count）
- ✅ 实现 `agent.py`：`LongMemoryAgent`，ask() 先检索长期记忆注入 context，再调工具，最后存入记忆
- ✅ 实现 `display.py`：检索结果展示（相似度分数）+ 注入摘要 + 短期vs长期对比表
- ✅ 实现 `main.py`：--demo（3 session 自动演示）/ --interactive（支持 reset/clear-memory）/ --clear-memory
- ✅ 复用 `knowledge_base.py` + `tools.py`（星际学院，来自 07）
- ✅ 通过全部验收核查：ChromaDB PersistentClient、相似度阈值过滤、空 collection 优雅处理、无本地模型依赖

### 学到的内容
- **RAG 在记忆系统中的应用**：向量检索 + 注入 context = 让 Agent "记住"过去的对话，本质是 Retrieval-Augmented Generation
- **ChromaDB 的 cosine distance**：返回的是距离（越小越相似），`similarity = 1 - distance` 才是相似度
- **自定义 EmbeddingFunction**：继承 `chromadb.EmbeddingFunction`，`__call__` 接收 `list[str]` 返回 `list[list[float]]`，完全通过 API，无本地模型
- **PersistentClient vs EphemeralClient**：PersistentClient 写磁盘，重启后数据保留；EphemeralClient 仅内存，程序退出即丢失
- **new_session 的关键设计**：只清 `_short_term`（当前 session messages），不动 ChromaDB，这才是"跨 session 记忆持久化"的本质
- **相似度阈值的工程意义**：无关历史记忆注入 context 会干扰 LLM 推理，threshold=0.7 是过滤噪声的实用值

### 遇到的问题
- **ChromaDB 默认 embedding 会下载本地模型**：不传 `embedding_function` 时 ChromaDB 自动用 `sentence-transformers/all-MiniLM-L6-v2`，必须显式传入自定义 EF
- **Collection 空时 query 崩溃**：`collection.query()` 在 `count()==0` 时抛异常，需要提前判断返回 `[]`

### 下一步计划
- 开始项目 `09-structured-output`（结构化输出，原合并项目已拆分为 09 + 10 两个独立项目）

### 今日耗时
- 需求脑暴 + 实现 + 验证：约 2 小时

---

## 2026-07-02（周四）完成 09-structured-output 🎉

### 今日完成
- ✅ 项目拆分：将原 `09-structured-output-and-planning` 拆为独立的 09（结构化输出）+ 10（任务分解与目标树），总项目数 17→18
- ✅ 需求脑暴：确定游戏工作室「星火互娱」知识库 + 3 种输出模式 × 4 层提取难度对比矩阵
- ✅ 实现 `knowledge_base.py`：29 个实体（工作室/项目组/游戏/开发者/技术栈/里程碑），多层关系
- ✅ 实现 `schemas.py`：4 层 Pydantic Model + `get_json_schema()` + `_enforce_strict_schema()`（递归处理 $defs）
- ✅ 实现 `extractor.py`：3 种输出模式（json_schema 强制 / json_object 弱 / text 纯文本）+ 重试机制（max 3）
- ✅ 实现 `display.py`：4×3 对比矩阵 ANSI 着色 + 成功率/平均重试统计
- ✅ 实现 `main.py`：--compare（12 组）/ --demo（6 组）/ --interactive
- ✅ 通过全部验收：json_schema 100% 校验通过、refusal 处理、JSON 语法错误处理

### 学到的内容
- **OpenAI Structured Outputs strict 模式**：受约束解码保证输出严格匹配 schema，但要求 `additionalProperties: false` + 所有属性进 `required`
- **Pydantic v2 的 $defs 机制**：嵌套 BaseModel 定义放在 `$defs`，主 schema 通过 `$ref` 引用，strict 处理必须递归到 $defs
- **三种模式的可靠性梯度**：json_schema（0 重试，100%）> json_object（需校验+重试）> text（最不可靠，需从自由文本提取 JSON）
- **重试反馈的关键**：把 ValidationError 提取成可读错误信息追加到 messages，让 LLM 定向修正，而非无脑重试
- **refusal 字段**：json_schema 模式下模型拒绝时填充 `message.refusal` 而非 `content`，需防崩溃处理

### 遇到的问题
- **Pydantic 直接生成的 schema 无法过 strict 校验**：需 `_enforce_strict_schema()` 递归补 `additionalProperties: false` 和 `required`
- **text 模式 JSON 提取格式多样**：纯 JSON / markdown 代码块 / 解释文本+JSON，需多策略解析

### 下一步计划
- 开始项目 `10-planning-and-goal-tree`（任务分解与目标树）

### 今日耗时
- 拆分规划 + 需求脑暴 + 实现 + 验证：约 3 小时

---

## 2026-07-06（周一）完成 10-planning-and-goal-tree 🎉

### 今日完成
- ✅ 需求脑暴：确定太空基地「新曙光基地」场景 + 混合规划模式 + DAG 目标树 + 局部重规划
- ✅ 实现 `knowledge_base.py`：32 个实体（资源8/模块8/设备6/团队5/环境事件5），含可变状态 + 依赖关系
- ✅ 实现 `schemas.py`：Plan / SubTask / SubTaskExpansion / ReplanResult 四模型（复用 09 的 json_schema 强制模式）
- ✅ 实现 `goal_tree.py`：运行时 DAG 结构，Kahn 拓扑排序 + 环检测 + 子树替换 + 依赖重定向
- ✅ 实现 `executor.py`：内层 ReAct 循环执行单个子任务，report_result 收尾工具判定成败
- ✅ 实现 `planner.py`：生成计划 / 局部重规划 / 动态展开（json_schema 强制）
- ✅ 实现 `planner_agent.py`：外层 Plan→Execute→Re-plan 编排循环 + 三重终止保护
- ✅ 实现 `display.py`：ASCII 目标树 + 依赖缩进 + 进度条 + ReAct 执行链着色
- ✅ 实现 `main.py`：--demo（默认自动跑完整流程）/ --interactive（自定义目标）
- ✅ 验证：goal_tree/knowledge_base/schemas 真实跑通，executor/planner_agent mock client 全链路通过

### 学到的内容
- **Plan→Execute→Re-plan 双层认知架构**：外层规划 DAG，内层 ReAct 执行单任务，区别于 03（单步推理）和 04（重试修正）
- **压平的 DAG 优于嵌套树**：用 `depends_on` 字段表达跨分支依赖，嵌套 JSON 无法表达；靠拓扑排序 + depth 计算还原树形展示
- **Kahn 拓扑排序天然带环检测**：排序节点数 < 总节点数即存在环，无需额外算法
- **局部重规划的核心是依赖重定向**：失败子任务拆成子树后，原来依赖它的下游任务要自动重定向到新子树的出口节点
- **用结构化收尾工具判定成败**：report_result(success, reason) 比解析自然语言可靠，reason 直接作为重规划的失败输入
- **把"部分完成状态"编码给 LLM**：重规划时用 `to_display_rows()` + `get_progress()` 把当前树状态文本化，让 LLM 只重规划受影响子树

### 遇到的问题
- **动态展开导致 parent 状态回退**：子任务展开出新子节点后，父节点状态管理需谨慎，避免已完成状态被覆盖
- **ANSI 着色破坏 ASCII 树对齐**：颜色码计入字符串宽度导致列错乱，需固定宽度或手动对齐
- **无限循环风险**：需 max_replans + 无 ready 任务检测 + 总迭代上限三重兜底
- **executor verbose 与 display 职责重叠**：主循环硬编码 `verbose=False`，执行渲染统一交给 display，避免双打印（executor verbose 保留供模块独立自测）

### 下一步计划
- 开始项目 `11-hitl-agent`（Human-in-the-Loop：主动中断 + 人类反馈 + 恢复执行）

### 今日耗时
- 需求脑暴 + 分批实现 + 验证：约 3.5 小时

---

## 2026-07-09（周三）完成 11-hitl-agent 🎉

### 今日完成
- ✅ 需求脑暴：确定明川市 6.8 级地震复合灾害场景 + rule-based HITL 触发 + 三层反馈 + 预设剧本 demo
- ✅ 实现 `knowledge_base.py`：虚构明川市状态（5 灾区/5 队伍/5 物资/5 基础设施），含 tick() 灾害恶化机制
- ✅ 实现 `schemas.py`：ApprovalType / HITLRequest / FeedbackType / HITLResponse / ActionResult Pydantic 模型
- ✅ 实现 `tools.py`：9 个工具（查询类不需审批 / 执行类 4 个需审批），requires_approval + approval_type 元数据
- ✅ 实现 `hitl.py`：HITLCheckpoint dataclass + HITLHandler ABC + ScriptedHandler + InteractiveHandler + MAX_REJECT_RETRIES=3
- ✅ 实现 `agent.py`：ReAct + HITL 主循环（approve 执行/reject 注入/info 注入），tick() 推进灾害，display 集成
- ✅ 实现 `display.py`：黄色 HITL 检查点区 vs 绿色自主执行区 + 灾害恶化红色警报
- ✅ 实现 `scenarios.py`：5 步演示剧本（approve + provide_info + reject + approve × 2）
- ✅ 实现 `main.py`：--demo / --interactive 双模式
- ✅ 修复 6 类 bug，通过全部 8 模块集成验证

### 学到的内容
- **HITL 的核心价值**：不是让 Agent 更智能，而是在不可逆/高风险操作上提供人类否决权；确定性 rule-based 比 LM 自判断更可靠
- **reject 的正确处理**：不是"修改参数重跑"，而是"把否决原因注入 messages"，让 LM 知道计划被否定并重新推理
- **tick() 的时机**：每个"步骤"（不论 approve/reject/info）都要推进灾害时间——reject 和 info 也消耗时间，人类拖延有代价
- **工具层 → Agent 层接口设计**：工具返回 `{success, message, alerts}` dict，Agent 层负责把 message+alerts 拼成字符串给 LM；两层的职责边界要提前约定

### 遇到的问题
- **dict vs list 迭代陷阱**：knowledge_base 状态全为 dict（以名称为 key），但 tools.py 按 list 迭代，运行时 TypeError
- **多处字段名不一致**：`trapped_people` vs `trapped`、`severity` vs `risk_level`、`water_level_m`（米）vs `water_level`（百分比）等 7 处字段名错误
- **execute_tool 返回 dict 被当 str 用**：OpenAI messages content 字段只接受 str，需提取 `result["message"]`
- **HITLRequest vs HITLCheckpoint 字段名混用**：display.py 按错误字段名访问，运行时 AttributeError

### 下一步计划
- 开始项目 `12-framework-and-observability`（LangGraph / OpenAI Agents SDK 框架对照 + tracing/eval）

### 今日耗时
- 跨两天（07-07 脑暴+设计，07-09 实现+修复+收尾）：约 4 小时

---

## 2026-07-09（周三）完成 12-framework-and-observability 🎉

### 今日完成
- ✅ 从 projects/03-react-agent/ 复制 knowledge_base.py（原封不动）
- ✅ 实现 tools.py：保留 execute_tool/TOOLS_SCHEMA + 新增 4 个 LangChain @tool 版本（lc_tools 列表）
- ✅ 实现 agent_v1_handwritten.py：从 03 react_agent.py 复用（纯文本 ReAct，无框架）
- ✅ 实现 agent_v2_stategraph.py：手动 StateGraph + ToolNode + InMemorySaver（~35 行）
- ✅ 实现 agent_v3_prebuilt.py：create_react_agent（~5 行核心）
- ✅ 实现 display.py：ANSI 着色（v1蓝/v2绿/v3青），对比表格，多轮分隔，LangSmith 状态提示
- ✅ 实现 main.py：--demo / --compare / --version v1/v2/v3 三种模式
- ✅ 修复 create_react_agent 参数名变更（state_modifier → prompt，langgraph 1.x）
- ✅ 端到端验证：v2/v3 用 deepseek-v4-flash 实际运行通过
- ✅ 完善文档：README.md（三路对比表）+ notes.md（踩坑）+ notes/langgraph-pattern.md（知识笔记）

### 学到的内容
- **StateGraph 声明式图**：把手写 for 循环 + messages.append + execute_tool 三件事变成两个节点 + 一条条件边，代码量降 75%
- **add_messages reducer**：LangGraph 状态管理的核心，节点 return 返回"增量"而非全量，框架负责合并
- **ToolNode 自动分发**：遍历 AIMessage.tool_calls → 找工具 → 执行 → 返回 ToolMessage，一行代替十几行
- **Checkpointer 多轮对话**：InMemorySaver 把 state 快照绑在图对象上，同一 graph 实例 + 同一 thread_id = 多轮连续上下文
- **LangSmith 零代码集成**：对 LangChain 模块只需环境变量，无需 @traceable 或 wrap_openai
- **两代 ReAct 范式**：v1 纯文本（Thought 显式输出，正则解析）vs v2/v3 Function Calling（结构化，推理隐式），不是替代而是演进

### 遇到的问题
- **create_react_agent 参数名变更**：研究文档写的是 state_modifier，实装的 langgraph 1.2.8 已改为 prompt，用 inspect.signature() 确认
- **InMemorySaver 必须用同一 graph 实例**：每次 build_graph() 都创建新 InMemorySaver，历史 checkpoint 不延续，demo 多轮对话需手动在同一实例上两次 invoke
- **模型可用性**：.env 里的 glm-5.1 端点下线，需先 client.models.list() 查可用模型再更新配置

### 下一步计划
- 开始 `13-observability`（Agent 可观测性：LangSmith tracing + eval，仍属阶段 2）

### 今日耗时
- 实现 + 验证：约 2 小时

---

## 2026-07-10（周四）完成 13-observability 🎉

### 今日完成
- ✅ 实现 `knowledge_base.py`：从 03 原封不动复制（星云大陆，~25 个实体）
- ✅ 实现 `tools.py`：三套接口——execute_tool（手写 Agent）+ @traceable 装饰器（LangSmith 手动接入）+ @tool 版本（LangGraph 使用）
- ✅ 实现 `agent_handwritten.py`：从 03 复用 ReAct 循环，接入 wrap_openai + custom_tracer 手动调用
- ✅ 实现 `agent_langgraph.py`：从 12 复用 StateGraph + ToolNode，零代码追踪
- ✅ 实现 `custom_tracer.py`：AgentTracer 类，on_llm_start/end + on_tool_start/end + on_agent_step，终端 ANSI 着色 + JSON 导出
- ✅ 实现 `evaluators.py`：规则匹配评分器（关键词匹配 pass/fail）+ LLM-as-Judge（1-5 分 + 三维度）
- ✅ 实现 `eval_runner.py`：加载 eval_dataset.json + 批量运行 Agent + 双评分器 + 汇总统计
- ✅ 实现 `eval_dataset.json`：8 个问题（5 事实型 + 3 开放型），关键词精确对应知识库数据
- ✅ 实现 `display.py`：eval 表格 + Agent 对比表格 + LangSmith 状态提示
- ✅ 实现 `main.py`：--trace / --eval / --compare / --demo 四种 CLI 模式
- ✅ LangSmith 可选降级：@traceable 和 wrap_openai 在未安装/未配置时是 no-op
- ✅ 完善文档：README.md + notes.md + notes/observability-pattern.md

### 学到的内容
- **Tracing 三层递进**：零代码（LangChain 模块 + 环境变量）→ 手动接入（wrap_openai + @traceable）→ 自定义（手写 tracer），三种方式各有适用场景
- **自定义 Tracer 的本质**：在代码关键点插入记录调用（on_llm_start/end、on_tool_start/end），不需要任何框架，一个类几个方法就够了
- **Eval 双评分器互补**：规则匹配（确定性、零成本、事实型）+ LLM-as-Judge（灵活、有成本、开放型），事实型问题两者都跑
- **wrap_openai 的价值**：一行代码让原生 openai SDK 获得 LangSmith 追踪能力，返回值不变，调用方无感知
- **降级设计模式**：try-except import + 环境变量检测 + 可选参数，三种组合确保可观测性工具不影响核心功能
- **@traceable 装饰业务函数而非入口函数**：这样无论从 execute_tool 还是 @tool 入口调用，LangSmith 都能捕获工具 span

### 遇到的问题
- **eval_dataset.json 关键词精度**：expected_keywords 必须精确匹配知识库数据（120000 而不是 12 万），否则规则匹配会误判
- **LLM-as-Judge 输出解析**：LLM 可能输出纯 JSON、markdown 代码块包裹的 JSON、或文字混合 JSON，需多策略提取
- **ANSI 颜色码影响字符串对齐**：颜色码计入字符串宽度导致表格列错乱，需手动处理对齐

### 下一步计划
- 阶段 2 全部完成 ✅，开始阶段 3：`14-mcp-tool-discovery`（MCP 动态工具发现）

### 今日耗时
- 实现 + 验证 + 文档：约 2 小时

---

## 2026-07-11（周五）完成 14-mcp-tool-discovery 🎉

### 今日完成
- ✅ 需求脑暴：确定星际探索指挥中心场景 + 多 Server 架构 + 三阶段 demo + 只做手写 Agent
- ✅ 安装 `mcp==1.28.1`（MCP Python SDK）
- ✅ 实现 `knowledge_base.py`：三领域数据（星图 9 实体 + 舰队 7 实体 + 通讯 5 条），含查询接口
- ✅ 实现 `server_starmap.py`：MCP Server A（`@mcp.tool()` + stdio），暴露 2 个星图工具
- ✅ 实现 `server_fleet.py`：MCP Server B（`@mcp.tool()` + stdio），暴露 2 个舰队工具
- ✅ 实现 `server_comms.py`：MCP Server C（`@mcp.tool()` + stdio），暴露 3 个通讯工具（动态加载）
- ✅ 实现 `mcp_client.py`：多 Server 连接 + AsyncExitStack 生命周期 + 工具聚合 + schema 转换 + 路由
- ✅ 实现 `agent_static.py`：静态工具 Agent（硬编码 TOOLS_SCHEMA，基线对照）
- ✅ 实现 `agent_mcp.py`：MCP 动态工具 Agent（list_tools → Function Calling）
- ✅ 实现 `display.py`：ANSI 着色（静态蓝/MCP绿/热加载青）+ 工具发现展示 + 对比汇总表
- ✅ 实现 `main.py`：--demo（三阶段）/ --interactive 双模式
- ✅ 修复 anyio task scope 问题（AsyncExitStack 替代手动 __aenter__/__aexit__）
- ✅ 端到端验证：三阶段 demo 完整跑通
- ✅ 完善文档：README.md + notes.md + notes/mcp-tool-discovery.md

### 学到的内容
- **MCP 协议三件事**：发现（list_tools）+ 调用（call_tool）+ 通知（tool_list_changed），这是相对于静态注册的核心价值
- **FastMCP + @mcp.tool()**：高级 API 一行创建 Server，装饰器自动生成 JSON Schema（和 LangChain @tool 机制相同）
- **MCP schema ≈ OpenAI schema**：两者都是标准 JSON Schema，转换只需把 inputSchema 搬到 function.parameters，无需深度转换
- **多 Server 工具路由**：Client 维护 tool_name→server_name 映射，Agent 不需要知道工具在哪个 Server
- **AsyncExitStack 管理 anyio 上下文**：MCP SDK 底层用 anyio TaskGroup，不能跨 task 退出 cancel scope，必须用 ExitStack 统一管理
- **热加载的本质**：新 Server 连接后 get_openai_tools() 列表自动变长，Agent 代码零修改

### 遇到的问题
- **anyio RuntimeError**：手动保存 async context manager 引用，在不同 task 中 __aexit__ 导致 "Attempted to exit cancel scope in a different task"。用 AsyncExitStack 解决
- **MCP Server INFO 日志混入输出**：Server 的 INFO 日志输出到 stderr，和 demo 输出混在一起。可设 log_level 或重定向 stderr
- **搜索关键词空格拼接**：`search_comms("赤焰星 紧急")` 返回空，因为是全文 `in` 匹配。LLM 第二次自动只搜"紧急"，展示了 Agent 推理纠错

### 下一步计划
- 开始 `15-multi-agent-debate`（辩论模式：多 Agent 投票决策）

### 今日耗时
- 需求脑暴 + 实现 + 验证 + 文档：约 2.5 小时

---

## 2026-07-11（周五）完成 15-multi-agent-debate 🎉

### 今日完成
- ✅ 需求脑暴：确定星际殖民委员会场景 + 三位专家辩论 + 共享+专属工具混合 + 三阶段辩论
- ✅ 实现 `knowledge_base.py`：三颗候选星球（蓝晶星/赤焰星/翡翠星），多维度评估数据
- ✅ 实现 `tools.py`：共享工具（search/lookup）+ 专属工具（analyze_habitability/assess_defense/evaluate_economics）
- ✅ 实现 `agents.py`：三角色定义（系统提示 + ReAct 循环 + 带重试的 LLM 调用）
- ✅ 实现 `orchestrator.py`：DebateOrchestrator 三阶段编排 + 消息路由 + 投票解析（JSON + 启发式降级）
- ✅ 实现 `display.py`：ANSI 着色（科学官青/军事官红/经济官黄）+ 投票汇总表 + 立场变化追踪
- ✅ 实现 `main.py`：--demo / --interactive 双模式
- ✅ 修复 .env 变量名不一致（OPENAI_MODEL vs MODEL_NAME）
- ✅ 添加 API 重试机制（max_retries=3 + 递增等待）+ max_tokens=2048 防止代理超时
- ✅ 端到端验证：三阶段完整辩论跑通，科学官立场改变（翡翠星→蓝晶星），全票通过蓝晶星
- ✅ 完善文档：README.md + notes.md + notes/multi-agent-debate.md

### 学到的内容
- **多 Agent 辩论核心模式**：独立推理 → 交叉质疑 → 收敛共识，信息可见性递增
- **信息隔离实现**：不是权限控制，而是控制 messages 列表里有什么——不传入不该看到的信息
- **共享+专属工具**：创造"共同的事实基础 + 不同的分析视角"，辩论更有实质性分歧
- **投票解析容错**：LLM 输出不稳定，需 JSON → 裸 JSON → 启发式三级降级
- **立场变化 = 辩论有效**：科学官被军事官和经济官说服改投蓝晶星，证明交叉质疑机制有效
- **API 代理超时处理**：大量工具调用产生超大上下文，max_tokens 限制 + retry + 控制 max_turns 是必要的

### 遇到的问题
- **API 代理 524 超时**：Agent 做了 10+ 次工具调用，上下文过大导致 Cloudflare 120s 代理超时。解决：max_turns 从 10 降到 4，max_tokens=2048，加 retry
- **API 503 资源耗尽**：代理服务 worker 达到请求上限。解决：retry 机制（30s/60s 递增等待）
- **.env 变量名不一致**：.env 是 OPENAI_MODEL，代码读 MODEL_NAME。统一为 OPENAI_MODEL

### 下一步计划
- 开始 `16-code-review-team`（代码审查团队：分工协作模式）

### 今日耗时
- 需求脑暴 + 实现 + 调试 API 超时 + 验证 + 文档：约 3 小时

---

## 2026-07-20（周六）完成 16-code-review-team 🎉

### 今日完成
- ✅ 需求脑暴：确定代码审查团队场景 + 两层协作架构 + 4 专业审查员 + 虚构代码库
- ✅ 实现 `code_samples.py`：4 段预设问题代码（A 安全/B 性能/C 架构/D 规范），24 个预设问题
- ✅ 实现 `schemas.py`：ReviewResult + Finding Pydantic 模型 + strict schema 生成（复用项目 09）
- ✅ 实现 `agents.py`：5 个 Agent（4 审查员 + 1 主审），各有专属 system prompt + 执行函数
- ✅ 实现 `orchestrator.py`：两层协作编排（并行执行层 + 汇总决策层）
- ✅ 实现 `display.py`：ANSI 着色（5 种颜色）+ 汇总表 + 召回率对比
- ✅ 实现 `main.py`：--demo / --interactive 双模式
- ✅ 修复敏感词过滤问题（硬编码密钥从 "sk-..." 改为明显虚构值）
- ✅ 端到端验证：4 审查员并行 + 主审汇总 + 去重排序完整流程跑通
- ✅ 完善文档：README.md + notes.md + notes/code-review-team.md

### 学到的内容
- **多 Agent 协作模式二：任务分解 + 并行聚合**：和项目 15 的"辩论投票"完全不同，15 是收敛型（多观点→一决策），16 是发散型（一任务→多维度→汇总）
- **两层协作架构**：执行层（专业 Agent 独立并行，json_schema 强制输出）+ 决策层（汇总 Agent 去重排序，自然语言报告）
- **结构化输出的工程价值**：执行层必须结构化（方便解析、去重、排序），决策层可以自然语言（面向人类）
- **虚构代码库设计原则**：单一维度集中（代码 A 主要安全问题）、问题可验证（准确行号+类型）、覆盖典型场景
- **去重逻辑**：主审用 LLM 做语义去重（比规则匹配更灵活，可识别不同表述的同一问题）
- **召回率验证**：预设问题 vs 实际发现，量化审查员准确性

### 遇到的问题
- **API 敏感词过滤**：硬编码密钥 "sk-1234567890abcdef" 被识别为真实密钥触发 500 错误，改为 "FAKE_API_KEY_DO_NOT_USE_IN_PRODUCTION" 解决

### 下一步计划
- 开始项目 `17-workflow-orchestration`（工作流编排引擎，阶段 3 最后一个项目）

### 今日耗时
- 需求脑暴 + 实现 + 验证 + 文档：约 3 小时

---

## 2026-07-20（周六）完成 17-workflow-orchestration 🎉

### 今日完成
- ✅ 需求脑暴：确定蓝晶星研究站场景 + 声明式 DSL + DAG 拓扑排序 + Context 自动注入
- ✅ 实现 `knowledge_base.py`：蓝晶星数据（3 地形区域 + 资源分布 + 环境数据）
- ✅ 实现 `tools.py`：5 个领域工具（地质勘探/选址分析/基建规划/能源设计/生命支持）
- ✅ 实现 `agents.py`：5 个专家 Agent 定义（地质学家/建筑师/工程师/能源专家/生命支持专家）
- ✅ 实现 `workflow.py`：Workflow 类 + Task 类 + DAG 拓扑排序（Kahn 算法）+ Context 自动注入
- ✅ 实现 `display.py`：ASCII 流程图可视化 + 依赖树展示 + 进度跟踪
- ✅ 实现 `main.py`：--demo（完整工作流）/ --interactive（自定义工作流）/ --visualize（仅可视化）
- ✅ 端到端验证：5 专家协作完成蓝晶星研究站规划（地质勘探 → 选址 → 基建 → 能源 + 生命支持）
- ✅ 完善文档：README.md + notes.md + notes/workflow-orchestration.md

### 学到的内容
- **声明式 DSL 的核心价值**：用户只需声明"做什么"和"依赖谁"，引擎自动推导执行顺序，无需手动编排
- **Kahn 拓扑排序算法**：维护入度计数 + 队列，每次取入度为 0 的节点执行，执行后减少下游节点入度
- **Context 自动注入机制**：下游任务自动获得上游输出作为 context，Agent 无需显式传递，降低耦合
- **可视化增强可理解性**：ASCII 流程图 + 依赖树展示，让用户一眼看清任务关系和执行顺序
- **并行执行的天然支持**：入度为 0 的节点可并行执行，但本项目为简化演示采用串行（并行需异步改造）

### 遇到的问题
- **循环依赖检测**：拓扑排序天然检测环（排序后剩余节点 = 有环），已在 `_topological_sort` 中抛出异常
- **Context 注入格式**：上游输出直接拼接成"=== 任务名 输出 ==="分隔的文本块，简单但有效

### 下一步计划
- 🎉 **阶段 3 全部完成**！接下来规划阶段 4（领域应用与优化）

### 今日耗时
- 需求脑暴 + 实现 + 验证 + 文档：约 2.5 小时

---

## 统计

- 本月学习天数：8 天（07-02 完成 08+09，07-06 完成 10，07-09 完成 11+12，07-10 完成 13，07-11 完成 14+15，07-20 完成 16+17）
- 完成项目：10 个（08-17，共 10 个项目）
- **完成阶段**：阶段 1 全部 ✅ + 阶段 2 全部 ✅ + 阶段 3 全部 ✅
- 笔记数量：主题笔记 17 篇 + 项目笔记 17 篇 = 共 34 篇
